import fs from 'fs';
import Message from './../../models/coupon';
import { mapMessage } from './../../utils/mapping';

module.exports = {

    getCoupon: async (req, res, next) => {
    },

    recivedCoupon: async (req, res, next) => {
    },

    getCouponDetail: async (req, res, next) => {
    },

    applyCoupon: async (req, res, next) => {
    },
}