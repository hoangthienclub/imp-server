<%- JSON.stringify({
	area: pageData.area,
	chinhSachUuDai: pageData.chinhSachUuDai,
	coCauSuDungDat: pageData.coCauSuDungDat,
	diaHinhDiaChat: pageData.diaHinhDiaChat,
	districtName: pageData.districtData ? pageData.districtData.name : "",
	coordinates: pageData.geometry ? pageData.geometry.coordinates : [],
	giaDien: pageData.giaDien,
	giaNuoc: pageData.giaNuoc,
	haTangKyThuat: pageData.haTangKyThuat,
	heThongDien: pageData.heThongDien,
	heThongGiaoThong: pageData.heThongGiaoThong,
	heThongNuoc: pageData.heThongNuoc,
	heThongVienThong: pageData.heThongVienThong,
	heThongXuLyNuocThai: pageData.heThongXuLyNuocThai,
	khiHau: pageData.khiHau,
	name: pageData.name,
	nganhNgheTiepNhan: pageData.nganhNgheTiepNhan,
	owner: pageData.owner,
	phiXuLyNuocThai: pageData.phiXuLyNuocThai,
    tiLeThue: pageData.tiLeThue,
    employee: pageData.employee,
	tienIchCongCong: pageData.tienIchCongCong,
    logo: pageData.logoImageId ? {
        name: pageData.logoImageId.name.trim(),
        url: pageData.logoImageId.url.trim()
    } : {name: "", url: ""},
	province: {
		name: pageData.provinceData.name,
		url: pageData.provinceData.url
	},
    images: pageData.imageIds && pageData.imageIds.length > 0 ? pageData.imageIds.map(e => {
        return {
            name: e.name.trim(),
            url: e.url.trim()
        };
    }) : [],
	address: {
		raw: pageData.address.raw,
		latLng: pageData.address.latLng
	},
    news: pageData.newsData.map(e => {
        return {
            logo: e.imageIds ? {
                name: e.imageIds.name.trim(),
                url: e.imageIds.url.trim()
            } : {name: "", url: ""},
            title: e.title,
            date: e.date,
            desc: e.desc
        }
    }),
	companies: pageData.companyData.map(e => {
        return {
            _id: e._id,
            address: {
				raw: e.address.raw, 
				latLng: e.address.latLng
			},
            provinceId: e.address.provinceId,
            provinceName: e.provinceData.name,
            logo: e.logoImageId ? {
                name: e.logoImageId.name.trim(),
                url: e.logoImageId.url.trim()
            } : {name: "", url: ""},
            name: e.name,
            url: e.url,
            taxNumber: e.taxNumber,
            categories: e.categories.map(f => {
                return {
                    name: f.name,
                    url: genUrl.getMapUrl(f.name)
                }
            })
        }
    }),
    industrialPark: pageData.relatedIpData.map(e => {
        return {
            _id: e._id,
			logoImageId: e.logoImageId ? e.logoImageId.url : "",
			name: e.name,
			area: e.area,
			tiLeThue: e.tiLeThue,
			address: e.address.raw,
			url: e.url
		}
    }),
    totalCompanies: pageData.totalCompany
}) %>