<%- JSON.stringify({
	name: pageData.name,
	desc: pageData.desc,
	email: pageData.email,
	phoneNumber: pageData.phoneNumber,
    fax: pageData.fax,
    website: pageData.website,
    foundDate: pageData.foundDate,
    registrationNumber: pageData.registrationNumber,
    president: pageData.president,
    vicePresident: pageData.vicePresident,
    secretary: pageData.secretary,
    logo: pageData.logoImageId ? {
        name: pageData.logoImageId.name.trim(),
        url: pageData.logoImageId.url.trim()
    } : {name: "", url: ""},
	address: {
        raw: pageData.address.raw,
        latLng: pageData.address.latLng
    },
    images: pageData.imageIds.length > 0 ? pageData.imageIds.map(e => {
        return {
            name: e.name.trim(),
            url: e.url.trim()
        };
    }) : [],
    news: pageData.newsData.map(e => {
        return {
            logo: e.imageIds ? {
                name: e.imageIds.name.trim(),
                url: e.imageIds.url.trim()
            } : {name: "", url: ""},
            title: e.title,
            date: e.date,
            desc: e.desc
        }
    }),
	companies: pageData.companyData.map(e => {
        return {
            _id: e._id,
            address: e.address.raw,
            provinceId: e.address.provinceId,
            provinceName: e.provinceData.name,
            logo: e.logoImageId ? {
                name: e.logoImageId.name.trim(),
                url: e.logoImageId.url.trim()
            } : {name: "", url: ""},
            name: e.name,
            url: e.url,
            taxNumber: e.taxNumber,
            categories: e.categories.map(f => {
                return {
                    name: f.name,
                    url: genUrl.getMapUrl(f.name)
                }
            })
        }
    })
}) %>